los ficheros .opt contienen colisiones texturas y otras muchas cosas
los ficheros .hm contienen el mapa de alturas y punto.
del opt:
el offset 0x00 -> 0x04 contiene vt01 y el juego original contiene vtop el online reconoce vtop como obsoleto. y != de vt01 corrupto.
el offset 0x08 -> 0x12 si es diferente de 0 hace El Operador Bitwise << 2
const buffer = Buffer.from([0xA2, 0x09, 0x00, 0x00]);
const numObjetos = buffer.readInt32LE(0); // Resultado: 2466

// El equivalente al malloc en JS simplemente sería preparar un array
const listaObjetos = new Array(numObjetos);


//hex vertigo offset BC328F2
00 00 86 20 28 00 86 20 28 00 07 AD B5 06 00 DF 63 3C

//length vertigo 
86 20 28 00 BigEndian
00282086

40CE1
01ce0400
//offset inicio vertigo
07ADB506 BigEndian
06B5AD07
//length.pista
40CE1